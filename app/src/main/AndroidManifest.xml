<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mercadolibre.test">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/SplashTheme"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".Activities.MainActivity"
            android:configChanges="screenSize|orientation|screenLayout|navigation">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.ItemDetailActivity"
            android:configChanges="screenSize|orientation|screenLayout|navigation"></activity>
    </application>
</manifest>